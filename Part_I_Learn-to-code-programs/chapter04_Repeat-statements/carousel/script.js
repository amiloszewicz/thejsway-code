// https://github.com/bpesquet/thejsway/blob/master/manuscript/chapter04.md#carousel

const launchCarousel = Number(prompt('How many times will you launch carousel?'));
for (let i = 1; i <= launchCarousel; i++) {
    console.log(`Carousel launch: ${i} times!`);
}
